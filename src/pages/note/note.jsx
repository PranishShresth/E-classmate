import React, { Fragment } from 'react';

import { Typography } from '@material-ui/core';

const Note = () => (
    <Fragment>
        <Typography>
            This is note page.
        </Typography>
    </Fragment>
);

export default Note;