import React, { Fragment } from 'react';

import { Typography } from '@material-ui/core';

const Chat = () => (
    <Fragment>
        <Typography>
            This is chat page.
        </Typography>
    </Fragment>
);

export default Chat;